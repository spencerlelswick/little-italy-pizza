<%- include('../partials/header') %>
  <h1>
    <%= title %>
  </h1>
  <p>
    <%= title %>
  </p>

  <%if (order){%>
    <p>current total: <%- order.total %></p>
    <% order.items.pizzas.forEach(p=>{ %>
      <p>pizza: <%- p %></p>
      <%} )}%>

        <a href=""></a>
        </br>
        </br>
        <a href="/order/builder">Add & Customize</a>

        <%- include('../partials/footer') %>